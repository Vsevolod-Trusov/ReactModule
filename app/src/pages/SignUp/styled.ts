import styled from 'styled-components';
import { Box } from '@mui/material';

export const StyledInputsWrapper = styled(Box)({
  display: 'flex',
  minWidth: '50vw',
  justifyContent: 'space-evenly',
});
