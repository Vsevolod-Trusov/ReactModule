import styled from 'styled-components';
import { Box } from '@mui/material';

const StyledInputAndErrorWrapper = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});

export { StyledInputAndErrorWrapper };
